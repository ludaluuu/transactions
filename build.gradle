plugins {
    id 'java'
}

group = 'com.example'
version = '0.0.1-SNAPSHOT'

compileJava {
    sourceCompatibility = '21'
}

allprojects {
    repositories {
        mavenCentral()
    }

    tasks.withType(Test.class).tap {
        configureEach {
            useJUnitPlatform()
        }
    }
}